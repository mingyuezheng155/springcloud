server:
  port: 8762
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka #eureka注册地址
#    fetch-registry: true
#    register-with-eureka: true
#  instance:
#      prefer-ip-address: true
spring:
  application:
    name: client
